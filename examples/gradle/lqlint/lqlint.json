{
  "fail-fast": false,
  "rules": {
    "create-index-name": {
      "pattern": "^{{value}}_IX\\d$\\d$",
      "dynamic-value": "tableName",
      "error-message": "Index names must be the table name, suffixed with 'IX' and a two-digit number, e.g. FOO_IX01"
    }
  }
}
